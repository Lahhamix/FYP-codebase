<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:name=".SolemateApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.Test.NoActionBar">
        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.Test.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".RegistrationActivity"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".ScanActivity"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".ReadingsActivity"
            android:label="Readings"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".GaitAnalysisActivity"
            android:label="Gait Analysis"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".PressureMatrixActivity"
            android:label="Pressure Matrix"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".PlantarFootAnalyticsActivity"
            android:label="Plantar Foot"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".BigToeAnalyticsActivity"
            android:label="Big Toe"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".AnkleCuffAnalyticsActivity"
            android:label="Ankle Cuff"
            android:theme="@style/Theme.Test.NoActionBar" />
        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:theme="@style/Theme.Test.NoActionBar" />

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Test.NoActionBar" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

</manifest>